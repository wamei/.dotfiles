\typeout{修論オプション : saruta@cs.titech.ac.jp}

%% 謝辞 再定義
\def\thanks#1{%
 \chapter*{謝辞\@mkboth{謝辞}{謝辞}}%
 \list{[\arabic{enumi}]}%
  {\settowidth\labelwidth{[#1]}%
  \leftmargin=\labelwidth
  \advance \leftmargin by \labelsep
  \usecounter{enumi}}%
 \addcontentsline{toc}{chapter}{{謝 辞}}%
 \def\newblock{\hskip .11em plus .33em minus .07em}
 \sloppy
 \sfcode`\.=1000\relax}%
\let\endthebibliography=\endlist

%% 参考文献 再定義
\def\thebibliography#1{%
 \chapter*{参考文献\@mkboth{参考文献}{参考文献}}%
 \list{[\arabic{enumi}]}%
  {\settowidth\labelwidth{[#1]}%
  \leftmargin=\labelwidth
  \advance \leftmargin by \labelsep
  \usecounter{enumi}}%
 \addcontentsline{toc}{chapter}{{参 考 文 献}}%
 \def\newblock{\hskip .11em plus .33em minus .07em}
 \sloppy
 \sfcode`\.=1000\relax}%
\let\endthebibliography=\endlist

%% section 再定義
\def\@chapapp#1#2{第#1#2章}
%\def\@chapapp#1#2{第#1#2}

%% enumerate 再定義
\def\theenumi{\arabic{enumi}} 
\def\labelenumii{\arabic{enumi}.\arabic{enumii}}
\def\theenumii{\arabic{enumi}.\arabic{enumii}}
\def\p@enumii{\theenumi}
\def\labelenumiii{(\alph{enumiii})}
\def\theenumiii{\alph{enumiii}}
\def\p@enumiii{\theenumi(\theenumii)}
\def\labelenumiv{\roman{enumiv}.}
\def\theenumiv{\roman{enumiv}}
\def\p@enumiv{\p@enumiii\theenumiii}

%% caption 再定義
\def\fnum@figure{図 \thefigure}
\def\fnum@table{表 \thetable}

\long\def\@makecaption#1#2{
 \vskip 10pt 
 \setbox\@tempboxa\hbox{#1\ \ #2}%  ``:''を入れるのをやめた
 \ifdim \wd\@tempboxa >\hsize \unhbox\@tempboxa\par \else \hbox
to\hsize{\hfil\box\@tempboxa\hfil} 
 \fi}

%%ページスタイルの変更．by hiroki
%%   ページ番号をフッターの中心に
%%   章の名前をヘッダーの右側に 
\def\ps@headings{\let\@mkboth\markboth
\def\@oddfoot{\hfil \rm\thepage \hbox{}\sl \hfil }
\def\@evenfoot{\hfil \rm\thepage \hbox{}\sl \hfil}
\def\@oddhead{\hfil \rightmark \hbox {}\sl}
\def\chaptermark##1{\markright {\uppercase{\ifnum \c@secnumdepth
      >\m@ne
      \@chapapp\ \thechapter. \ \fi ##1}}}}
%      \@chapapp\ \thechapter \ \fi ##1}}}}

\def\ps@myheadings{\let\@mkboth\@gobbletwo
\def\@oddhead{\hfil\rightmark\hbox{}\sl}
\def\@oddfoot{}
\def\@evenhead{\hfil\sl\leftmark\hbox{}}
\def\@evenfoot{}\def\chaptermark##1{}%
\def\sectionmark##1{}\def\subsectionmark##1{}}

%%
%% 左綴じだからちょっとだけ右に寄せて文字をふやす
%%
%\addtolength{\textwidth}{4zw}

%%「Appendix」を「付録」に変更
%% by hiroa
%%
\def\appendix{\par
 \setcounter{chapter}{0}
 \setcounter{section}{0}
 \def\@chapapp{付録}
 \def\thechapter{\Alph{chapter}}}

% Local variables:
% file-coding-system: *junet*
% End:

 