[tools]
node = "26"
bun = "1"
ruby = "3.1.2"
python = "3.13.3"
# sqls (SQL の言語サーバ、Emacs の eglot から使う)。配布バイナリが x86_64 のみで
# Rosetta 頼みになるため go backend で arm64 にビルドする。go はそのためのツール。
go = "1"
"go:github.com/sqls-server/sqls" = "latest"

# npm パッケージのグローバル導入。プロジェクトの依存には入らないが PATH から引きたい
# 道具 (Emacs の eglot / apheleia が起動する言語サーバとフォーマッタ、単体 CLI) を
# ここに集める。実体の取得は bun (下の npm.package_manager)。
"npm:@biomejs/biome" = "latest"
"npm:prettier" = "latest"
# TypeScript 7 の tsc は native バイナリ (typescript-go) で、`tsc --lsp -stdio` が
# そのまま言語サーバになる (init.el の eglot ブロックがこれを起動する)。そのため
# typescript-language-server は入れない。
# npm backend は install 先の node_modules/.bin も PATH に出すので、
# vscode-langservers-extracted が依存で抱える typescript 4.9.5 の tsc / tsserver まで
# 漏れてくる。PATH の優先順はこのファイルの記述順なので、tsc を 7 系にするには
# typescript を vscode-langservers-extracted より前に置く (入れ替えると 4.9.5 が勝つ)。
"npm:typescript" = "latest"
"npm:vscode-langservers-extracted" = "latest"
"npm:@prisma/language-server" = "latest"
# Emacs の eglot が起動する言語サーバ。dockerfile-language-server-nodejs の bin は
# docker-langserver で、どちらも eglot 組み込みのエントリがそのまま使う名前。
"npm:dockerfile-language-server-nodejs" = "latest"
"npm:yaml-language-server" = "latest"
"npm:@redocly/cli" = "latest"
"npm:macos-audio-devices" = "latest"
"npm:@google/clasp" = "latest"
# 静的ファイルのライブリロードサーバ (VSCode の live-server 拡張と同じ用途)。本家
# live-server は 2022 年から止まっているので後継の five-server を使う。bin に
# five-server と live-server の両方が入るので `live-server <dir>` で引ける。
# package.json の engines は ^18.19.0 だが node 26 で起動・リロードとも確認済み。
# 依存の html-validate が bin を持つため PATH に漏れるが、既存コマンドと衝突しない。
"npm:five-server" = "latest"

# brew から mise へ移した CLI。バイナリ配布があり aqua registry に載っている
# ものだけを移し、zsh プラグイン (share/ 配下を .zshrc が source する) や
# ライブラリ (libpq)、registry に無いもの (timg / wget / nkf / screen) と
# tap のソースビルド (macism) は brew に残してある (mise.toml 側で宣言)。
"aqua:uutils/coreutils" = "latest"
"aqua:direnv/direnv" = "latest"
"aqua:cli/cli" = "latest"              # gh
"aqua:git-lfs/git-lfs" = "latest"
"aqua:jqlang/jq" = "latest"
"aqua:BurntSushi/ripgrep" = "latest"
"aqua:tmux/tmux-builds" = "latest"
"aqua:pypa/pipx" = "latest"
"aqua:aws/aws-cli" = "latest"
# brew の copilot は AWS Copilot CLI (ECS/Fargate)。mise registry の
# "copilot" は GitHub Copilot CLI で別物なので、こちらを明示する。
"aqua:aws/copilot-cli" = "latest"
# ghq。リポジトリの置き場を ~/projects/<host>/<owner>/<repo> に揃える
# (root は .gitconfig の [ghq] で指定)。
"aqua:x-motemen/ghq" = "latest"
# java を裸で書くと 17 系が 17.0.2 (2022 年で更新停止の OpenJDK GA ビルド)
# に解決され、brew の openjdk@17 (17.0.20.1) から 3 年分のパッチが失われる。
# temurin を明示すると 17.0.20+101 になり同世代を保てる。この version pin は
# 今も必要。
# ただし backend 名 (core:) を付けても `mise ls --current` はこのエントリを
# 裸の `java` として報告するため、.zshrc の show_env_mise が持つ
# 「<backend>:<pkg> 形式はプロンプトから除く」フィルタはここには効かない。
# 実際に `mise ls --current` は `java  temurin-17.0.20+101  ...` と表示され、
# 結果として今回の移行で `java:temurin-17.0.20+101` が全プロンプトに新しく
# 出るようになった (以前は brew の PATH から java が来ており mise はそれを
# 見ていなかったため出ていなかった)。抑制したい場合は show_env_mise 側で
# java を明示的に除外する必要がある (このファイルではなく .zshrc の変更)。
"core:java" = "temurin-17"
"pipx:azure-cli" = "latest"
# brew の formula 名は cloudflare-wrangler だが npm パッケージ名は wrangler。
# 実体の取得は下の npm.package_manager (bun)。
"npm:wrangler" = "latest"

# YAML の lint。Emacs の yaml-ts-mode 同梱の flymake backend が PATH の yamllint を
# 起動する (見つからないと error を投げるので init.el 側にガードあり)。Python 製で
# バイナリ配布が無いため pipx backend で入れる (実体の取得は /opt/homebrew/bin/pipx)。
"pipx:yamllint" = "latest"

# 他人の repo が pnpm / yarn 前提のときに使う。pnpm は aqua backend のネイティブ
# バイナリで node を要らないのでそちらから入れる。yarn は既定 (vfox) と aqua が
# berry (4 系) なので、classic を使い続けるため npm backend で 1 系を指定する。
# どちらも backend を明示するのは、.zshrc の show_env_mise が backend 付き
# (<backend>:<pkg>) を「言語のバージョンではない道具」としてプロンプトから除くため。
"aqua:pnpm/pnpm" = "latest"
"npm:yarn" = "1"

[settings]
# nodenv / rbenv / pyenv と同じく .node-version / .nvmrc / .ruby-version / .python-version を読む
# (mise は既定では無視する)
idiomatic_version_file_enable_tools = ["node", "ruby", "python"]
# npm backend が実際に叩くパッケージマネージャ。既定の "auto" だと npm が使われる。
npm.package_manager = "bun"
# バージョン一覧の取得を npm CLI に任せる。mise 内蔵の取得だと packument が大きい
# パッケージ (@prisma/language-server は 25MB) で fetch_remote_versions_timeout に
# 間に合わず "latest" が解決できない。取得だけ npm、インストールは上の bun。
npm.shell_out = true
